# ===========================================
# 环境变量配置模式
# ===========================================
# 此文件用于演示如何通过环境变量配置
# 可以通过以下方式启动：
# npm run dev:env
# 或者直接设置环境变量：
# VITE_PORT=3000 VITE_GLOB_APP_TITLE="My App" npm run dev

# 基础配置 - 这些值可以通过环境变量覆盖
VITE_PORT=${VITE_PORT:-3100}
VITE_PUBLIC_PATH=${VITE_PUBLIC_PATH:-/}
VITE_GLOB_APP_TITLE=${VITE_GLOB_APP_TITLE:-ThingsBoard UI Vue}
VITE_GLOB_APP_SHORT_NAME=${VITE_GLOB_APP_SHORT_NAME:-thingsboard_ui_vue}

# 路由配置
VITE_ROUTE_WEB_HISTORY=${VITE_ROUTE_WEB_HISTORY:-true}

# API 配置 - 可以通过环境变量动态设置
VITE_PROXY=${VITE_PROXY:-[["/api","http://127.0.0.1:8888/api",false]]}
VITE_GLOB_API_URL=${VITE_GLOB_API_URL:-}
VITE_GLOB_API_URL_PREFIX=${VITE_GLOB_API_URL_PREFIX:-}
VITE_GLOB_API_URL_WEBSOCKET=${VITE_GLOB_API_URL_WEBSOCKET:-/api/ws}

# 构建配置
VITE_DROP_CONSOLE=${VITE_DROP_CONSOLE:-false}
VITE_OUTPUT_DIR=${VITE_OUTPUT_DIR:-dist}
VITE_BUILD_COMPRESS=${VITE_BUILD_COMPRESS:-none}
VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE=${VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE:-false}
VITE_USE_PWA=${VITE_USE_PWA:-false}
VITE_LEGACY=${VITE_LEGACY:-true}

# 其他配置
REPORT=${REPORT:-false}
VITE_CJS_IGNORE_WARNING=${VITE_CJS_IGNORE_WARNING:-true}
