// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

import { describe, it, expect } from 'vitest';
import {

// Build configuration mocks
Object.defineProperty(globalThis, '__COLOR_PLUGIN_OUTPUT_FILE_NAME__', {
  value: 'mock-theme.css', writable: true
});
  ALarmShowStatus,
  AlarmStatus,
  AlarmSeverity,
  ALARM_SHOW_STATUS_OPTIONS,
  ALARM_STATUS_OPTIONS,
  ALARM_SEVERITY_OPTIONS,
  AlarmConditionKeyType,
  ALARM_CONDITION_KEY_TYPE_OPTIONS,
  AlarmConditionValueType,
  ALARM_CONDITION_VALUE_TYPE_OPTIONS,
  PredicateOperation,
  PREDICATE_OPERATION_OPTIONS,
} from '/@/enums/alarmEnum';

describe('enums/alarmEnum', () => {
  describe('ALarmShowStatus', () => {
    it('should define all alarm show status values', () => {
      expect(ALarmShowStatus.ACTIVE_UNACK).toBe('ACTIVE_UNACK');
      expect(ALarmShowStatus.ACTIVE_ACK).toBe('ACTIVE_ACK');
      expect(ALarmShowStatus.CLEARED_UNACK).toBe('CLEARED_UNACK');
      expect(ALarmShowStatus.CLEARED_ACK).toBe('CLEARED_ACK');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(ALarmShowStatus);
      expect(values).toHaveLength(4);
    });
  });

  describe('AlarmStatus', () => {
    it('should define all alarm status values', () => {
      expect(AlarmStatus.ACTIVE).toBe('ACTIVE');
      expect(AlarmStatus.CLEARED).toBe('CLEARED');
      expect(AlarmStatus.ACK).toBe('ACK');
      expect(AlarmStatus.UNACK).toBe('UNACK');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(AlarmStatus);
      expect(values).toHaveLength(4);
    });
  });

  describe('AlarmSeverity', () => {
    it('should define all alarm severity values', () => {
      expect(AlarmSeverity.CRITICAL).toBe('CRITICAL');
      expect(AlarmSeverity.MAJOR).toBe('MAJOR');
      expect(AlarmSeverity.MINOR).toBe('MINOR');
      expect(AlarmSeverity.WARNING).toBe('WARNING');
      expect(AlarmSeverity.INDETERMINATE).toBe('INDETERMINATE');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(AlarmSeverity);
      expect(values).toHaveLength(5);
    });
  });

  describe('AlarmConditionKeyType', () => {
    it('should define all alarm condition key types', () => {
      expect(AlarmConditionKeyType.ATTRIBUTE).toBe('ATTRIBUTE');
      expect(AlarmConditionKeyType.TIME_SERIES).toBe('TIME_SERIES');
      expect(AlarmConditionKeyType.CONSTANT).toBe('CONSTANT');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(AlarmConditionKeyType);
      expect(values).toHaveLength(3);
    });
  });

  describe('AlarmConditionValueType', () => {
    it('should define all alarm condition value types', () => {
      expect(AlarmConditionValueType.STRING).toBe('STRING');
      expect(AlarmConditionValueType.NUMERIC).toBe('NUMERIC');
      expect(AlarmConditionValueType.BOOLEAN).toBe('BOOLEAN');
      expect(AlarmConditionValueType.DATE_TIME).toBe('DATE_TIME');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(AlarmConditionValueType);
      expect(values).toHaveLength(4);
    });
  });

  describe('PredicateOperation', () => {
    it('should define all predicate operation values', () => {
      expect(PredicateOperation.EQUAL).toBe('EQUAL');
      expect(PredicateOperation.NOT_EQUAL).toBe('NOT_EQUAL');
      expect(PredicateOperation.STARTS_WITH).toBe('STARTS_WITH');
      expect(PredicateOperation.ENDS_WITH).toBe('ENDS_WITH');
      expect(PredicateOperation.CONTAINS).toBe('CONTAINS');
      expect(PredicateOperation.NOT_CONTAINS).toBe('NOT_CONTAINS');
      expect(PredicateOperation.IN).toBe('IN');
      expect(PredicateOperation.NOT_IN).toBe('NOT_IN');
      expect(PredicateOperation.LESS).toBe('LESS');
      expect(PredicateOperation.GREATER).toBe('GREATER');
      expect(PredicateOperation.GREATER_OR_EQUAL).toBe('GREATER_OR_EQUAL');
      expect(PredicateOperation.LESS_OR_EQUAL).toBe('LESS_OR_EQUAL');
    });

    it('should have the correct number of enum values', () => {
      const values = Object.values(PredicateOperation);
      expect(values).toHaveLength(12);
    });
  });

  describe('ALARM_SHOW_STATUS_OPTIONS', () => {
    it('should define all alarm show status options', () => {
      expect(ALARM_SHOW_STATUS_OPTIONS).toHaveLength(4);

      const activeUnackOption = ALARM_SHOW_STATUS_OPTIONS.find((opt) => opt.value === ALarmShowStatus.ACTIVE_UNACK);
      expect(activeUnackOption).toBeDefined();
      expect(activeUnackOption?.label).toBe('激活未确认');

      const activeAckOption = ALARM_SHOW_STATUS_OPTIONS.find((opt) => opt.value === ALarmShowStatus.ACTIVE_ACK);
      expect(activeAckOption).toBeDefined();
      expect(activeAckOption?.label).toBe('激活已确认');

      const clearedUnackOption = ALARM_SHOW_STATUS_OPTIONS.find((opt) => opt.value === ALarmShowStatus.CLEARED_UNACK);
      expect(clearedUnackOption).toBeDefined();
      expect(clearedUnackOption?.label).toBe('清理未确认');

      const clearedAckOption = ALARM_SHOW_STATUS_OPTIONS.find((opt) => opt.value === ALarmShowStatus.CLEARED_ACK);
      expect(clearedAckOption).toBeDefined();
      expect(clearedAckOption?.label).toBe('清理已确认');
    });

    it('should have correct structure for each option', () => {
      ALARM_SHOW_STATUS_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });

  describe('ALARM_STATUS_OPTIONS', () => {
    it('should define all alarm status options', () => {
      expect(ALARM_STATUS_OPTIONS).toHaveLength(4);

      const activeOption = ALARM_STATUS_OPTIONS.find((opt) => opt.value === AlarmStatus.ACTIVE);
      expect(activeOption).toBeDefined();
      expect(activeOption?.label).toBe('已激活');

      const clearedOption = ALARM_STATUS_OPTIONS.find((opt) => opt.value === AlarmStatus.CLEARED);
      expect(clearedOption).toBeDefined();
      expect(clearedOption?.label).toBe('已清理');

      const ackOption = ALARM_STATUS_OPTIONS.find((opt) => opt.value === AlarmStatus.ACK);
      expect(ackOption).toBeDefined();
      expect(ackOption?.label).toBe('已确认');

      const unackOption = ALARM_STATUS_OPTIONS.find((opt) => opt.value === AlarmStatus.UNACK);
      expect(unackOption).toBeDefined();
      expect(unackOption?.label).toBe('未确认');
    });

    it('should have correct structure for each option', () => {
      ALARM_STATUS_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });

  describe('ALARM_SEVERITY_OPTIONS', () => {
    it('should define all alarm severity options', () => {
      expect(ALARM_SEVERITY_OPTIONS).toHaveLength(5);

      const criticalOption = ALARM_SEVERITY_OPTIONS.find((opt) => opt.value === AlarmSeverity.CRITICAL);
      expect(criticalOption).toBeDefined();
      expect(criticalOption?.label).toBe('危险');
      expect(criticalOption?.color).toBe('#FF0000');

      const majorOption = ALARM_SEVERITY_OPTIONS.find((opt) => opt.value === AlarmSeverity.MAJOR);
      expect(majorOption).toBeDefined();
      expect(majorOption?.label).toBe('重要');
      expect(majorOption?.color).toBe('#E37318');

      const minorOption = ALARM_SEVERITY_OPTIONS.find((opt) => opt.value === AlarmSeverity.MINOR);
      expect(minorOption).toBeDefined();
      expect(minorOption?.label).toBe('次要');
      expect(minorOption?.color).toBe('#FFA000');

      const warningOption = ALARM_SEVERITY_OPTIONS.find((opt) => opt.value === AlarmSeverity.WARNING);
      expect(warningOption).toBeDefined();
      expect(warningOption?.label).toBe('警告');
      expect(warningOption?.color).toBe('#FFCA3D');

      const indeterminateOption = ALARM_SEVERITY_OPTIONS.find((opt) => opt.value === AlarmSeverity.INDETERMINATE);
      expect(indeterminateOption).toBeDefined();
      expect(indeterminateOption?.label).toBe('不确定');
    });

    it('should have correct structure for each option', () => {
      ALARM_SEVERITY_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });

  describe('ALARM_CONDITION_KEY_TYPE_OPTIONS', () => {
    it('should define all alarm condition key type options', () => {
      expect(ALARM_CONDITION_KEY_TYPE_OPTIONS).toHaveLength(3);

      const attributeOption = ALARM_CONDITION_KEY_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionKeyType.ATTRIBUTE,
      );
      expect(attributeOption).toBeDefined();
      expect(attributeOption?.label).toBe('属性');

      const timeSeriesOption = ALARM_CONDITION_KEY_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionKeyType.TIME_SERIES,
      );
      expect(timeSeriesOption).toBeDefined();
      expect(timeSeriesOption?.label).toBe('遥测数据');

      const constantOption = ALARM_CONDITION_KEY_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionKeyType.CONSTANT,
      );
      expect(constantOption).toBeDefined();
      expect(constantOption?.label).toBe('常量');
    });

    it('should have correct structure for each option', () => {
      ALARM_CONDITION_KEY_TYPE_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });

  describe('ALARM_CONDITION_VALUE_TYPE_OPTIONS', () => {
    it('should define all alarm condition value type options', () => {
      expect(ALARM_CONDITION_VALUE_TYPE_OPTIONS).toHaveLength(4);

      const stringOption = ALARM_CONDITION_VALUE_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionValueType.STRING,
      );
      expect(stringOption).toBeDefined();
      expect(stringOption?.label).toBe('字符串');

      const numericOption = ALARM_CONDITION_VALUE_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionValueType.NUMERIC,
      );
      expect(numericOption).toBeDefined();
      expect(numericOption?.label).toBe('数字');

      const booleanOption = ALARM_CONDITION_VALUE_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionValueType.BOOLEAN,
      );
      expect(booleanOption).toBeDefined();
      expect(booleanOption?.label).toBe('布尔值');

      const dateTimeOption = ALARM_CONDITION_VALUE_TYPE_OPTIONS.find(
        (opt) => opt.value === AlarmConditionValueType.DATE_TIME,
      );
      expect(dateTimeOption).toBeDefined();
      expect(dateTimeOption?.label).toBe('日期时间');
    });

    it('should have correct structure for each option', () => {
      ALARM_CONDITION_VALUE_TYPE_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });

  describe('PREDICATE_OPERATION_OPTIONS', () => {
    it('should define all predicate operation options', () => {
      expect(PREDICATE_OPERATION_OPTIONS).toHaveLength(12);

      const equalOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.EQUAL);
      expect(equalOption).toBeDefined();
      expect(equalOption?.label).toBe('等于');

      const notEqualOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.NOT_EQUAL);
      expect(notEqualOption).toBeDefined();
      expect(notEqualOption?.label).toBe('不等于');

      const startsWithOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.STARTS_WITH);
      expect(startsWithOption).toBeDefined();
      expect(startsWithOption?.label).toBe('开始于');

      const endsWithOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.ENDS_WITH);
      expect(endsWithOption).toBeDefined();
      expect(endsWithOption?.label).toBe('结束于');

      const containsOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.CONTAINS);
      expect(containsOption).toBeDefined();
      expect(containsOption?.label).toBe('包含');

      const notContainsOption = PREDICATE_OPERATION_OPTIONS.find(
        (opt) => opt.value === PredicateOperation.NOT_CONTAINS,
      );
      expect(notContainsOption).toBeDefined();
      expect(notContainsOption?.label).toBe('不包含');

      const inOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.IN);
      expect(inOption).toBeDefined();
      expect(inOption?.label).toBe('匹配');

      const notInOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.NOT_IN);
      expect(notInOption).toBeDefined();
      expect(notInOption?.label).toBe('不匹配');

      const lessOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.LESS);
      expect(lessOption).toBeDefined();
      expect(lessOption?.label).toBe('小于');

      const greaterOption = PREDICATE_OPERATION_OPTIONS.find((opt) => opt.value === PredicateOperation.GREATER);
      expect(greaterOption).toBeDefined();
      expect(greaterOption?.label).toBe('大于');

      const greaterOrEqualOption = PREDICATE_OPERATION_OPTIONS.find(
        (opt) => opt.value === PredicateOperation.GREATER_OR_EQUAL,
      );
      expect(greaterOrEqualOption).toBeDefined();
      expect(greaterOrEqualOption?.label).toBe('大于等于');

      const lessOrEqualOption = PREDICATE_OPERATION_OPTIONS.find(
        (opt) => opt.value === PredicateOperation.LESS_OR_EQUAL,
      );
      expect(lessOrEqualOption).toBeDefined();
      expect(lessOrEqualOption?.label).toBe('小于等于');
    });

    it('should have correct structure for each option', () => {
      PREDICATE_OPERATION_OPTIONS.forEach((option) => {
        expect(option).toHaveProperty('value');
        expect(option).toHaveProperty('label');
        expect(typeof option.value).toBe('string');
        expect(typeof option.label).toBe('string');
      });
    });
  });
});
