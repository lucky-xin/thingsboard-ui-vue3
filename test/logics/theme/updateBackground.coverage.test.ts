// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock state management and global dependencies
vi.mock("/@/store", () => ({
  useAppStore: () => ({
    getTheme: vi.fn(() => "light"),
    setTheme: vi.fn(),
    locale: "en",
    setLocale: vi.fn()
  }),
  useUserStore: () => ({
    userInfo: { name: "Test User" },
    isLoggedIn: true
  })
}));

vi.mock("/@/hooks/setting/useLocale", () => ({
  useLocale: () => ({
    getLocale: vi.fn(() => ({ lang: "en" })),
    changeLocale: vi.fn(),
    t: vi.fn((key) => key)
  })
}));

// Mock build configuration for Vite plugins
Object.defineProperty(globalThis, '__COLOR_PLUGIN_OUTPUT_FILE_NAME__', {
  value: 'mock-theme-output.css',
  writable: true
});

Object.defineProperty(globalThis, '__PROD__', {
  value: false,
  writable: true
});

Object.defineProperty(globalThis, '__COLOR_PLUGIN_OPTIONS__', {
  value: {},
  writable: true
});

import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock dependencies
vi.mock('/@/utils/color', () => ({
  colorIsDark: vi.fn(),
  lighten: vi.fn(),
  darken: vi.fn(),
}));

vi.mock('/@/store/modules/app', () => ({
  useAppStore: vi.fn(),
}));

vi.mock('/@/enums/appEnum', () => ({
  ThemeEnum: {
    DARK: 'dark',
    LIGHT: 'light',
  },
}));

vi.mock('/@/logics/theme/util', () => ({
  setCssVar: vi.fn(),
}));

import { updateHeaderBgColor, updateSidebarBgColor } from '/@/logics/theme/updateBackground';
import { colorIsDark, lighten, darken } from '/@/utils/color';
import { useAppStore } from '/@/store/modules/app';
import { ThemeEnum } from '/@/enums/appEnum';
import { setCssVar } from '/@/logics/theme/util';

const mockColorIsDark = vi.mocked(colorIsDark);
const mockLighten = vi.mocked(lighten);
const mockDarken = vi.mocked(darken);
const mockUseAppStore = vi.mocked(useAppStore);
const mockSetCssVar = vi.mocked(setCssVar);

describe('updateBackground coverage', () => {
  let mockAppStore: any;

  beforeEach(() => {
    vi.clearAllMocks();

    // Mock app store
    mockAppStore = {
      getDarkMode: ThemeEnum.LIGHT,
      getHeaderSetting: {
        bgColor: '#ffffff',
      },
      getMenuSetting: {
        bgColor: '#001529',
      },
      setProjectConfig: vi.fn(),
    };
    mockUseAppStore.mockReturnValue(mockAppStore);

    // Mock color utilities
    mockColorIsDark.mockReturnValue(false);
    mockLighten.mockReturnValue('#f0f0f0');
    mockDarken.mockReturnValue('#d0d0d0');
  });

  describe('updateHeaderBgColor', () => {
    it('should update header background color with provided color', () => {
      const testColor = '#ff0000';

      updateHeaderBgColor(testColor);

      expect(mockSetCssVar).toHaveBeenCalledWith('--header-bg-color', testColor);
      expect(mockDarken).toHaveBeenCalledWith(testColor, 5);
      expect(mockSetCssVar).toHaveBeenCalledWith('--header-bg-hover-color', '#d0d0d0');
      expect(mockSetCssVar).toHaveBeenCalledWith('--header-active-menu-bg-color', '#d0d0d0');
      expect(mockColorIsDark).toHaveBeenCalledWith(testColor);
      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: {
          theme: ThemeEnum.LIGHT,
        },
      });
    });

    it('should use default color when no color provided in light mode', () => {
      updateHeaderBgColor();

      expect(mockSetCssVar).toHaveBeenCalledWith('--header-bg-color', '#ffffff');
      expect(mockDarken).toHaveBeenCalledWith('#ffffff', 5);
      expect(mockColorIsDark).toHaveBeenCalledWith('#ffffff');
    });

    it('should use dark mode color when in dark mode', () => {
      mockAppStore.getDarkMode = ThemeEnum.DARK;

      updateHeaderBgColor();

      expect(mockSetCssVar).toHaveBeenCalledWith('--header-bg-color', '#151515');
      expect(mockDarken).toHaveBeenCalledWith('#151515', 5);
      expect(mockColorIsDark).toHaveBeenCalledWith('#151515');
    });

    it('should set dark theme when color is dark', () => {
      mockColorIsDark.mockReturnValue(true);

      updateHeaderBgColor('#000000');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: {
          theme: ThemeEnum.DARK,
        },
      });
    });

    it('should set dark theme when in dark mode regardless of color darkness', () => {
      mockAppStore.getDarkMode = ThemeEnum.DARK;
      mockColorIsDark.mockReturnValue(false);

      updateHeaderBgColor('#ffffff');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: {
          theme: ThemeEnum.DARK,
        },
      });
    });

    it('should set light theme when color is light and not in dark mode', () => {
      mockAppStore.getDarkMode = ThemeEnum.LIGHT;
      mockColorIsDark.mockReturnValue(false);

      updateHeaderBgColor('#ffffff');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: {
          theme: ThemeEnum.LIGHT,
        },
      });
    });

    it('should handle different color formats', () => {
      const colors = ['#ff0000', '#00ff00', '#0000ff', 'rgb(255,0,0)', 'red'];

      colors.forEach((color) => {
        vi.clearAllMocks();
        updateHeaderBgColor(color);
        expect(mockSetCssVar).toHaveBeenCalledWith('--header-bg-color', color);
      });
    });
  });

  describe('updateSidebarBgColor', () => {
    it('should update sidebar background color with provided color', () => {
      const testColor = '#ff0000';

      updateSidebarBgColor(testColor);

      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', testColor);
      expect(mockDarken).toHaveBeenCalledWith(testColor, 6);
      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-darken-bg-color', '#d0d0d0');
      expect(mockLighten).toHaveBeenCalledWith(testColor, 5);
      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-lighten-bg-color', '#f0f0f0');
    });

    it('should use default color when no color provided in light mode', () => {
      updateSidebarBgColor();

      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', '#001529');
      expect(mockDarken).toHaveBeenCalledWith('#001529', 6);
      expect(mockLighten).toHaveBeenCalledWith('#001529', 5);
    });

    it('should use dark mode color when in dark mode', () => {
      mockAppStore.getDarkMode = ThemeEnum.DARK;

      updateSidebarBgColor();

      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', '#151515');
      expect(mockDarken).toHaveBeenCalledWith('#151515', 6);
      expect(mockLighten).toHaveBeenCalledWith('#151515', 5);
    });

    it('should set light theme for white colors when not in dark mode', () => {
      const whiteColors = ['#fff', '#ffffff', '#FFF', '#FFFFFF'];

      whiteColors.forEach((color) => {
        vi.clearAllMocks();
        mockAppStore.getDarkMode = ThemeEnum.LIGHT;

        updateSidebarBgColor(color);

        expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
          menuSetting: {
            theme: ThemeEnum.LIGHT,
          },
        });
      });
    });

    it('should set dark theme for white colors when in dark mode', () => {
      mockAppStore.getDarkMode = ThemeEnum.DARK;

      updateSidebarBgColor('#ffffff');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        menuSetting: {
          theme: ThemeEnum.DARK,
        },
      });
    });

    it('should set dark theme for non-white colors', () => {
      const nonWhiteColors = ['#ff0000', '#00ff00', '#0000ff', '#f0f0f0'];

      nonWhiteColors.forEach((color) => {
        vi.clearAllMocks();
        mockAppStore.getDarkMode = ThemeEnum.LIGHT;

        updateSidebarBgColor(color);

        expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
          menuSetting: {
            theme: ThemeEnum.DARK,
          },
        });
      });
    });

    it('should handle different color formats', () => {
      const colors = ['#ff0000', '#00ff00', '#0000ff', 'rgb(255,0,0)', 'red'];

      colors.forEach((color) => {
        vi.clearAllMocks();
        updateSidebarBgColor(color);
        expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', color);
      });
    });

    it('should handle case insensitive white color detection', () => {
      const whiteVariations = ['#fff', '#FFF', '#ffffff', '#FFFFFF', '#FfF', '#FfFfFf'];

      whiteVariations.forEach((color) => {
        vi.clearAllMocks();
        mockAppStore.getDarkMode = ThemeEnum.LIGHT;

        updateSidebarBgColor(color);

        expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
          menuSetting: {
            theme: ThemeEnum.LIGHT,
          },
        });
      });
    });

    it('should handle edge cases', () => {
      // Test with null/undefined
      updateSidebarBgColor(null as any);
      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', '#001529');

      // Test with empty string
      updateSidebarBgColor('');
      expect(mockSetCssVar).toHaveBeenCalledWith('--sider-dark-bg-color', '#001529');
    });
  });

  describe('integration tests', () => {
    it('should work together for theme switching', () => {
      mockAppStore.getDarkMode = ThemeEnum.DARK;
      mockColorIsDark.mockReturnValue(true);

      updateHeaderBgColor('#000000');
      updateSidebarBgColor('#000000');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledTimes(2);
      expect(mockSetCssVar).toHaveBeenCalledTimes(6); // 3 for header, 3 for sidebar
    });

    it('should handle theme switching from light to dark', () => {
      // Start in light mode
      mockAppStore.getDarkMode = ThemeEnum.LIGHT;
      mockColorIsDark.mockReturnValue(false);

      updateHeaderBgColor('#ffffff');
      updateSidebarBgColor('#ffffff');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: { theme: ThemeEnum.LIGHT },
      });
      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        menuSetting: { theme: ThemeEnum.LIGHT },
      });

      // Switch to dark mode
      mockAppStore.getDarkMode = ThemeEnum.DARK;
      mockColorIsDark.mockReturnValue(true);

      updateHeaderBgColor('#000000');
      updateSidebarBgColor('#000000');

      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        headerSetting: { theme: ThemeEnum.DARK },
      });
      expect(mockAppStore.setProjectConfig).toHaveBeenCalledWith({
        menuSetting: { theme: ThemeEnum.DARK },
      });
    });
  });
});
